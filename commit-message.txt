feat: agregar ErrorBoundary para evitar pantalla blanca

Se creó el componente `ErrorBoundary` que captura errores de render y muestra una UI amigable.
App.tsx ahora envuelve las rutas con este boundary, evitando que la aplicación quede en blanco ante excepciones no controladas.

Además se mejoró el manejo de datos:
- Se añadió `extractErrorMessage` y `comoArray` a `UbicacionSelects`, `CanjesPage` y `AdminFarmaciasPage`.
- Se actualizó la lógica de carga de provincias, cantones y distritos para convertir respuestas en arrays seguros. [ci skip]
